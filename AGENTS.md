# Codex Workflow Notes

- For running the example, do `swift build` first, then run the built binary directly: `.build/arm64-apple-macosx/debug/CoreMLToolsExample`.
- Avoid `swift run` inside Codex; use the compiled binary instead.
